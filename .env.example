# 复制本文件为 .env 并填上真实值；程序启动时会自动读取项目根目录的 .env。
# 本文件不会真正生效，只有 .env 会（.env 已被 .gitignore 忽略，不会提交）。

# 模型服务地址（兼容 OpenAI Chat Completions 协议），常见示例：
#   DeepSeek       https://api.deepseek.com/v1
#   OpenAI         https://api.openai.com/v1
#   本地 vLLM      http://localhost:8000/v1
#   本地 Ollama    http://localhost:11434/v1
RAG_BASE_URL=https://api.openai.com/v1

# API 密钥（本地模型可留空）
RAG_API_KEY=

# 模型名称，例如 deepseek-chat / gpt-4o-mini / 你本地服务的模型 ID
RAG_MODEL=
